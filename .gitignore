# values that make sense if you intend to turn this into a
# Git repository, or add it to an existing repo.

# our module puts a private ssh key into this folder.
.ssh

.terraform

*.tfvars
*.tfvars.json

terraform.tfstate*

plan